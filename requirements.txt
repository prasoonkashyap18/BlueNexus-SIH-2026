# Root-level dependency manifest for Vercel's Python builder, which looks for
# requirements.txt at the project root (see `main.py` and `vercel.json`).
#
# The real, maintained dependency list stays at `backend/requirements.txt` --
# this file only points to it via pip's `-r` include so there is one source
# of truth and nothing here can drift out of sync with local development.
-r backend/requirements.txt
