# Root-level dependency manifest for the Vercel deployment (see `main.py` and
# `vercel.json`). Vercel's Python builder reads requirements.txt from the
# project root and cannot follow `-r` includes, so the pins are listed here
# directly. They MUST stay identical to `backend/requirements.txt`, which
# remains the documented dependency list for local development.
fastapi==0.141.1
uvicorn[standard]==0.52.4
xarray==2026.7.0
netCDF4==1.7.4
gsw==3.6.23
