# ---------- Node / Frontend ----------
node_modules/
frontend/node_modules/
frontend/dist/
frontend/dist-ssr/
frontend/.vite/
*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
logs/
*.log

# Browser / test-automation scratch artifacts (headless-Chrome PID files, etc.)
*.pid

# ---------- Python / Backend ----------
backend/.venv/
.venv/
venv/
env/
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ---------- Data pipeline (D8+) ----------
# Regenerated on demand from data/raw/. Raw inputs under data/raw/ are tracked.
# data/bluenexus/*.bnx (D9 BlueNexus containers) are also tracked -- the
# read-only Vercel deployment filesystem cannot rebuild them at startup, so
# the validated pre-built artifacts must ship in the repository.
data/processed/*.d8.json

# ---------- Environment ----------
.env
.env.*
!.env.example

# ---------- Editors / OS ----------
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.swp
