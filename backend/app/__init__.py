"""BlueNexus backend package.

Currently holds the Data Integration Track pipeline code. The FastAPI service
in ``backend/main.py`` is intentionally left untouched by Data Track step D7 --
no API wiring happens until D10.
"""
